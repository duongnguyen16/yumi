import { spacing } from "../tokens";

export const workflowListLayout = {
  gap: 0,
  padding: 0,
  paddingBottom: spacing[6],
} as const;

export const workflowRowLayout = {
  iconSize: 40,
  minHeight: 76,
  paddingHorizontal: spacing[1],
} as const;
